--- a/include/libnftnl/expr.h
+++ b/include/libnftnl/expr.h
@@ -269,6 +269,7 @@ enum {
 	NFTNL_EXPR_MASQ_FLAGS		= NFTNL_EXPR_BASE,
 	NFTNL_EXPR_MASQ_REG_PROTO_MIN,
 	NFTNL_EXPR_MASQ_REG_PROTO_MAX,
+	NFTNL_EXPR_MASQ_FULLCONE,
 	__NFTNL_EXPR_MASQ_MAX
 };
 
--- a/include/linux/netfilter/nf_tables.h
+++ b/include/linux/netfilter/nf_tables.h
@@ -1499,12 +1499,14 @@ enum nft_tproxy_attributes {
  * @NFTA_MASQ_FLAGS: NAT flags (see NF_NAT_RANGE_* in linux/netfilter/nf_nat.h) (NLA_U32)
  * @NFTA_MASQ_REG_PROTO_MIN: source register of proto range start (NLA_U32: nft_registers)
  * @NFTA_MASQ_REG_PROTO_MAX: source register of proto range end (NLA_U32: nft_registers)
+ * @NFTA_MASQ_REG_FULLCONE: BCM fullcone NAT (NLA_U8)
  */
 enum nft_masq_attributes {
 	NFTA_MASQ_UNSPEC,
 	NFTA_MASQ_FLAGS,
 	NFTA_MASQ_REG_PROTO_MIN,
 	NFTA_MASQ_REG_PROTO_MAX,
+	NFTA_MASQ_REG_FULLCONE,
 	__NFTA_MASQ_MAX
 };
 #define NFTA_MASQ_MAX		(__NFTA_MASQ_MAX - 1)
--- a/src/expr/masq.c
+++ b/src/expr/masq.c
@@ -20,6 +20,7 @@ struct nftnl_expr_masq {
 	uint32_t		flags;
 	enum nft_registers	sreg_proto_min;
 	enum nft_registers	sreg_proto_max;
+	uint8_t			fullcone;
 };
 
 static int
@@ -38,6 +39,11 @@ nftnl_expr_masq_set(struct nftnl_expr *e
 	case NFTNL_EXPR_MASQ_REG_PROTO_MAX:
 		memcpy(&masq->sreg_proto_max, data, data_len);
 		break;
+	case NFTNL_EXPR_MASQ_FULLCONE:
+		if (data_len != sizeof(masq->fullcone))
+			return -1;
+		memcpy(&masq->fullcone, data, sizeof(masq->fullcone));
+		break;
 	}
 	return 0;
 }
@@ -58,6 +64,9 @@ nftnl_expr_masq_get(const struct nftnl_e
 	case NFTNL_EXPR_MASQ_REG_PROTO_MAX:
 		*data_len = sizeof(masq->sreg_proto_max);
 		return &masq->sreg_proto_max;
+	case NFTNL_EXPR_MASQ_FULLCONE:
+		*data_len = sizeof(masq->fullcone);
+		return &masq->fullcone;
 	}
 	return NULL;
 }
@@ -71,6 +80,10 @@ static int nftnl_expr_masq_cb(const stru
 		return MNL_CB_OK;
 
 	switch (type) {
+	case NFTA_MASQ_REG_FULLCONE:
+		if (mnl_attr_validate(attr, MNL_TYPE_U8) < 0)
+			abi_breakage();
+		break;
 	case NFTA_MASQ_REG_PROTO_MIN:
 	case NFTA_MASQ_REG_PROTO_MAX:
 	case NFTA_MASQ_FLAGS:
@@ -96,6 +109,8 @@ nftnl_expr_masq_build(struct nlmsghdr *n
 	if (e->flags & (1 << NFTNL_EXPR_MASQ_REG_PROTO_MAX))
 		mnl_attr_put_u32(nlh, NFTA_MASQ_REG_PROTO_MAX,
 				 htobe32(masq->sreg_proto_max));
+	if (e->flags & (1 << NFTNL_EXPR_MASQ_FULLCONE))
+		mnl_attr_put_u8(nlh, NFTA_MASQ_REG_FULLCONE, masq->fullcone);
 }
 
 static int
@@ -121,6 +136,10 @@ nftnl_expr_masq_parse(struct nftnl_expr
 			be32toh(mnl_attr_get_u32(tb[NFTA_MASQ_REG_PROTO_MAX]));
 		e->flags |= (1 << NFTNL_EXPR_MASQ_REG_PROTO_MAX);
 	}
+	if (tb[NFTA_MASQ_REG_FULLCONE]) {
+		masq->fullcone = mnl_attr_get_u8(tb[NFTA_MASQ_REG_FULLCONE]);
+		e->flags |= (1 << NFTNL_EXPR_MASQ_FULLCONE);
+	}
 
 	return 0;
 }
@@ -145,6 +164,11 @@ static int nftnl_expr_masq_snprintf(char
 		ret = snprintf(buf + offset, remain, "flags 0x%x ", masq->flags);
 		SNPRINTF_BUFFER_SIZE(ret, remain, offset);
 	}
+	if (e->flags & (1 << NFTNL_EXPR_MASQ_FULLCONE)) {
+		ret = snprintf(buf + offset, remain,
+			       "fullcone %u ", masq->fullcone);
+		SNPRINTF_BUFFER_SIZE(ret, remain, offset);
+	}
 
 	return offset;
 }
@@ -153,6 +177,7 @@ static struct attr_policy masq_attr_poli
 	[NFTNL_EXPR_MASQ_FLAGS]         = { .maxlen = sizeof(uint32_t) },
 	[NFTNL_EXPR_MASQ_REG_PROTO_MIN] = { .maxlen = sizeof(uint32_t) },
 	[NFTNL_EXPR_MASQ_REG_PROTO_MAX] = { .maxlen = sizeof(uint32_t) },
+	[NFTNL_EXPR_MASQ_FULLCONE]      = { .maxlen = sizeof(uint8_t) },
 };
 
 struct expr_ops expr_ops_masq = {
